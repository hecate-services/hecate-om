# Plan: hecate-om Mesh Wrappers (RPC provider-side, PubSub, Content)

**Status:** Pieces B and C implemented and tested, 2026-08-24 (uncommitted) — piece B verified fully callable end-to-end against a real demo-fleet station, which also surfaced and fixed two pre-existing bugs in `hecate_om_capabilities`'s consumer side (see piece B). D/E/F/G/H still design-only, not approved, no code written.
**Created:** 2026-08-24
**Supersedes (implementation phase of):** `plans/PLAN_MACULA_API_INTEGRATION_SURVEY.md` — that survey is done, its findings are folded in here, still worth reading for the six open questions it worked through.
**Read alongside:** `plans/PLAN_HECATE_OM_MESH_WRAPPERS_ROOT.md` — repo-by-repo context and the instruction to survey more repos before finalizing the open design questions below. Don't re-derive what's already in either doc.

---

## The one line this exists for

So `hecate_om` itself is built directly on macula's **supervised** primitives (`macula_client`, `macula_publisher`, `macula_subscriber`, `macula_response`/`macula_streamer` advertise, `macula_feeder`/`macula_download`) — not on raw `macula:publish/4`-style calls — and so every `hecate-services/hecate-X` provider gets that supervision for free, the same way it already gets connection lifecycle and RPC-consumer calls for free.

**The point of the 2026-08-24 session-2 survey was explicitly two-sided, not just cataloguing duplication:** (1) does the supervised API, read directly rather than assumed, already cover what ~20 hand-rolled implementations built for themselves — and where it does, hand-rolled code isn't evidence for the wrapper's *design*, it's a list of things the wrapper makes obsolete; (2) where the hand-rolled code reveals a real requirement the supervised primitive's own surface doesn't dictate (a realm parameter, an outcome-handling mode, a dynamic set of topics), that's a genuine design input for `hecate_om`'s wrapper API, not for macula itself. Getting this backwards — designing the wrapper to match what the hand-rolled code does, instead of what the supervised primitive already provides — was a real mistake made once in this plan's first draft of pieces C and D (see the "CORRECTED" notes below) and is exactly the failure mode this section exists to warn against.

## Why now, with evidence

- The original survey (2026-08-21/22) found four DIY modules hand-rolling raw `macula:subscribe`/`publish` because `hecate_om` gave them nothing, and one confirmed gap: `hecate_om_capabilities` only writes DHT discovery records, it never calls `advertise_direct`, so a service is discoverable but not callable.
- `hecate-services/hecate-tube` was deliberately built as one complete exemplar exercising all four primitive pairs (RPC, PubSub, Content, Streaming) against raw SDK primitives on purpose, specifically so this design would come from one real, finished service instead of fragments. It's now built and live (2026-08-23). Full audit in the handover doc.
- A workspace-wide grep across all 23 `hecate_om`-consuming repos (this session, 2026-08-24) found the pattern is far more widespread than the original four-module survey knew: **9 repos** with their own hand-rolled `advertise_direct`/`macula:advertise` provider wiring, **14** with their own `macula_subscriber`/`macula:subscribe`, **17+ repos** (24+ modules) with their own `macula_publisher`/`macula:publish`, and (so far) only `hecate-tube` with hand-rolled Content wrapping. Full list in the handover.
- Two independent implementations of "supervised advertise, with periodic re-assert" already exist (`hecate-tom-world`'s `tom_advertiser.erl` / `hecate-tom-ocean`'s `tom_ocean_mesh.erl`, and `hecate-tube`'s `tube_mesh_providers.erl`) and at least the latter encodes a genuinely hard-won, non-obvious lesson (below) that would otherwise get rediscovered the hard way by the next service that needs it.

## Scope: six pieces

Each piece: what it is, why, what to derive it from (concrete source, not guessed), the open question(s) that need settling before code, and how to know it's actually done (not just compiling).

### A. Connection lifecycle: `hecate_om_identity` onto `macula_client:child_spec/3`

**What:** Replace `hecate_om_identity`'s hand-rolled `self() ! connect` / 5s-retry / manual `erlang:monitor` + `DOWN` reconnect with `macula_client:child_spec/3`, embedded as a proper supervised child. Keep cert loading, keypair self-heal/persist/puzzle-harden, org/realm/CA resolution exactly as they are — those are Hecate identity concerns, not the SDK's job, and stay layered as a thin wrapper around the pool.

**Why:** Verified this session by reading `macula_client.erl` directly (not assumed): `connect/2` (the `child_spec/3` start MFA) returns immediately even with zero seeds connected; each seed link dials independently, retries forever on its own timer (`?LINK_RESPAWN_DELAY_MS`) without touching the supervisor, and never crashes the pool process for an unreachable seed. That's exactly the resilience `hecate_om_identity` hand-built, already free in the SDK. We cannot justify keeping two implementations of the same thing once one is confirmed to already do the job.

**Open question, must verify not assume:** `hecate_om_identity`'s own comment claims a real, previously-observed bug — "hecate_om may start before the macula SDK app is fully up, so a single inline connect races it and loses." `hecate_om.app.src` already lists `macula` in `applications`, so standard OTP boot ordering should prevent this in a real release boot. Whether that race is still real (and if so, in what boot path — release, `rebar3 shell`, CT) is only answerable by attempting the migration and trying to reproduce it, not by more reading. If it reproduces, that's a genuine macula-level finding worth raising upstream; if it doesn't, the migration is clean.

**Acceptance:** every existing `hecate_om_identity_tests.erl` case still passes; a boot with zero configured seeds still starts the service (degrades, doesn't crash); a boot with seeds present connects; a station restart mid-run reconnects without service restart; the boot-race question above is explicitly tested, not assumed away.

**Sequencing note:** do this **separately** from B–F below, not bundled in the same commit series. It touches the one thing every deployed `hecate-services/*` consumer already relies on; B–F are purely additive (new modules, new optional callbacks) and carry far less blast radius. Land B–F first, on the *current* connection layer, then do this migration as its own isolated slice with the full existing test suite as a regression net.

### B. RPC provider-side wiring in `hecate_om_capabilities` — IMPLEMENTED 2026-08-24 (uncommitted)

`src/hecate_om_capabilities.erl` rewritten (moduledoc, state, advertise-side internals), `src/hecate_om_service.erl`'s `capability()` type extended, `test/hecate_om_capabilities_tests.erl` extended — 17/17 tests passing (4 new), full suite 30 EUnit + 11 CT green, `rebar3 lint` clean.

**Design correction found while implementing, not caught by the survey:** the "does the RPC path need `advertise_direct` at all" question wasn't actually open — `hecate_om_capabilities`'s own `call_capability/5,7` (already shipped, unchanged by this piece) resolves providers from the DHT and dials them via `macula:call_station/6,7,8` — direct-dial, by design, not pool-routed `macula:call/4`. Per `RPC_GUIDE.md`, `call_station`-reachable handlers *require* `advertise_direct` registration ("a plain advertise publishes no discoverable record"). So the `dns`/`git`/`llm`/`rag` finding (plain `advertise/5`, no reconnect defense needed) was correct for *their* ad hoc, undiscoverable RPC modules, but doesn't apply to `hecate_om_capabilities` itself, which is architecturally committed to direct-dial. Read `src/macula_response.erl` directly to settle it: `advertise_direct/6,7` already does exactly what `hecate_om_capabilities` was hand-rolling *half* of — `advertise/6` (registers the handler, pool-stored) **then** `macula_direct_dial:publish_advertisement/5` (writes the same `procedure_advertisement` record `build_advertisement/5,6` was hand-building), plus the `reuse_sup` option (added to macula for exactly this re-advertise-without-leaking need) and a `cert_chain` option matching the existing Slice 7c dual-trust support. **So piece B isn't "add a handler call alongside the existing DHT write" — it's "replace the hand-rolled half with the one call that already does both halves correctly."** Keeping both would have recreated the exact two-sources-of-truth pattern the survey caught causing `hecate-rag`'s real dispatch-shape bug.

**What was built:** a capability carrying `handler => {Module, Args}` (`Module` implementing `-behaviour(macula_response)`) is advertised via `advertise_direct/7`, with each capability's factory-supervisor pid round-tripped through a new `advertise_sups` state field and passed back in as `reuse_sup` on every 30s republish tick — no supervisor leak. A capability with no `handler` key keeps the legacy record-only path unchanged, for backward compatibility. `has_handler/1` and `reuse_sup_opts/1` exported as pure, directly-testable dispatch helpers (same convention as this module's existing `procedure_uri/3`/`build_advertisement/5,6`, and as piece C's `resolve_realm/2`).

**The MFA-pair-not-closure requirement turned out to already be structurally satisfied, not something to add:** `macula_response:advertise/6` takes a `Module` atom + `Args`, dispatched fresh via `Module:init/1`/`Module:handle_request/2` *at each inbound call*, not a fun captured at advertise time — there is no closure to pin a stale code version in the first place. `tom_advertiser.erl`'s hand-rolled-fun problem doesn't exist when built on `macula_response`.

**Realm override:** already free — `advertise_direct/6,7` takes `Realm` as a plain argument, same as `macula_publisher` in piece C. No design work needed here either.

**Not yet done, deliberately out of scope for this pass, same reasoning as piece C:** porting `hecate-dns`/`-git`/`-llm`/`-rag`'s `*_mesh_rpc.erl` modules or `hecate-embedder`'s advertiser onto this. Also not done: `macula_streamer:advertise_direct/6,7` (the streaming half of the original piece B scope) — kept out of this pass; streaming has a different callback shape and no capability-declaration precedent yet in `hecate_om_service`, and folding it in would have doubled this piece's surface without new evidence to design it from (hecate-tube's `stream_video_clip_by_id.erl` is the only streaming-provider example in the whole survey). Worth its own follow-on once a second streaming exemplar exists.

**UPDATED, same session — the strong acceptance criterion is now fully met, against a real station, and it found two real pre-existing bugs.** Asked directly whether the demo fleet could just be used (it's documented, disposable dev infra — nothing was stopping this), so `test/hecate_om_capabilities_live_station_tests.erl` was written against `station-de-frankfurt.macula.io` (re-`dig`ged live, confirmed current — see `reference_demo_fleet_boxes` memory). First attempt failed, and root-causing it live surfaced two real, pre-existing bugs in `hecate_om_capabilities`'s already-shipped consumer side (`call_capability`), neither introduced by piece B's write-side work — piece B's live test was simply the first thing to ever exercise that code end-to-end:

1. **`resolve_at/4`/`resolve_full/4` keyed DHT lookups by `procedure_uri/3`** (Realm+Org+Name), **but a handler-bearing capability is written under macula's own `discovery_uri/2` key** (Realm+bare-Name — `macula_direct_dial.erl`'s own comment: "No Org segment... Org is only consulted post-resolve"). `call_capability`'s own dial side already used the bare name (`macula:call_station(..., CapName, ...)`), so the read side's Org-inclusive key was never going to match what a handler-bearing capability is actually reachable under. Fixed with a new `discovery_key/2` (exported, pure) replicating macula's private formula; `procedure_uri/3` and the legacy record-only path are untouched, so existing tests are unaffected.
2. **`dial_provider` called `macula:call_station/7` with no trust override**, so every direct-dial call failed at the TLS layer (`{error, not_connected}`, misleadingly generic) before the application-level trust check — the DHT record's pubkey signature — ever ran. `macula_direct_dial:call/6` (macula's own canonical resolve-then-call implementation) sets exactly `#{verify => none, pin_tls_cert => false, expected_node_id => Station}` for this reason, with a comment explaining why: "trust is pinned to the exact pubkey the signed DHT chain resolved, but enforced at the application layer, not the TLS layer — a production station's TLS certificate has no relationship to its macula identity." `hecate_om_capabilities` was missing this triad entirely. Fixed by matching macula's own convention exactly, `Station` now threaded through to `dial_provider`.

Both were diagnosed with real, live evidence (not guessed): `lookup/1` succeeding while `call_capability` failed with the same key isolated bug #1; direct debug instrumentation of `resolve_endpoint`/`macula:call_station`'s actual return values, plus ruling out same-identity-two-connections and DHT-propagation-timing as competing hypotheses (a separate consumer identity/pool made no difference; the record was stable across repeated `lookup/1` calls), isolated bug #2. Final test: two independent pools/identities (provider + consumer, matching a real deployment), full round trip, `{ok, #{echo := #{ping := <<"pong">>}}}` — note the reply keys arrive as atoms, confirming the session's separate atom-key wire finding live, for the first time end-to-end, as a bonus.

**Acceptance, now fully met:** a handler-bearing capability's `register/1`/`publish/0` reach `advertise_direct` and degrade cleanly without a station (unit-tested). `reuse_sup` is the exact pid from a prior call (pure-tested). **Genuinely callable mesh-to-mesh, from an independent consumer identity, against a real station — proven live.** 31 EUnit (was 30) + 11 CT, all green; `rebar3 lint` clean. The live test runs as part of the default `rebar3 eunit` (fast, ~1s) — not excluded, a deliberate tradeoff given this repo already treats the demo fleet as safe dev infra; move it to a separate profile if it ever proves flaky in CI, don't delete the coverage.

### C. `hecate_om_pubsub:publish/2,3` — IMPLEMENTED 2026-08-24 (uncommitted)

`src/hecate_om_pubsub.erl` + `test/hecate_om_pubsub_tests.erl` written and passing (9/9 new tests; full suite 26 EUnit + 11 CT, all green; `rebar3 lint` clean). Matches the corrected design below: `publish/2,3` and `publish_many/2,3`, built on `macula_publisher:start_link/6`, all three outcome modes, realm override. `resolve_realm/2`/`resolve_mode/1` exported as pure helpers (same convention as `hecate_om_capabilities.erl`) — needed because `macula_publisher` ships to hex without `debug_info`, so `meck`'s `passthrough` mode can't wrap it here; testing the resolution decision directly sidesteps that and is arguably the better test anyway. Live-outcome tests use a real zero-seed `macula_client:connect([], #{})` pool (no station needed) so `sync`/`async_log` are proven against a genuine SDK `{error, {transient, no_healthy_station}}`, not a stub — `async_log`'s warning was confirmed to actually fire in the test run.

**Not yet done, deliberately out of scope for this pass:** porting any of the ~20 hand-rolled call sites in the migration table below to actually call this. That's a separate, per-repo follow-on once C itself is reviewed — implementing the wrapper and proving its own behavior is a different unit of work from migrating consumers onto it. Also new to this repo: a `{profiles, [{test, [{deps, [meck]}]}]}` block in `rebar.config` (`~> 0.9`, matching macula's own pin) — worth knowing if another piece's tests want the same tool.

**Design (as implemented):**

**RE-CORRECTED, 2026-08-24 session 2 (second pass) — went one step too far the first time.** The first pass of this session concluded piece C should wrap raw synchronous `macula:publish/4` because ~20 hand-rolled repos do exactly that. That's backwards: those ~20 repos hand-roll raw calls *because `hecate_om` gives them nothing better*, not because `macula_publisher` (the SDK's own supervised behaviour, read directly from `src/macula_publisher.erl` this session) is unfit for the job. Reading it settles what actually needs designing:

```erlang
-callback init(Args) -> {ok, State} | {stop, Reason}.
-callback handle_published(Result :: ok | {error, term()}, State) ->
    {noreply, NewState} | {stop, Reason, NewState}.

macula_publisher:start_link(Module, Pool, Realm, Topic, Payload, Args)
```

It already takes `Pool`/`Realm`/`Topic` as explicit arguments (realm-override is not a gap to design, it's already there), delivers the outcome to a callback the *caller* fully controls (silent / log / return-to-caller are three different one-line `handle_published/2` bodies, not three designs to reconcile), supports cancellation, and — a real capability none of the ~20 hand-rolled call sites have today — auto-publishes `pubsub.publish_started_v1`/`pubsub.publish_completed_v1` lifecycle facts (with `outcome => completed | failed | cancelled`) around every publish, for free. **What:** `hecate_om_pubsub:publish/2,3,4` starts a `macula_publisher` with a default callback module `hecate_om`'s own `handler => sync | async_log | async_silent` option selects (or a caller-supplied module for anything else), reading `mesh_handles/0` internally for Pool and defaulting Realm to `hecate_om_identity:realm()`.

**What the survey actually contributes here — real requirements for the *wrapper's calling convention*, not for the SDK primitive:**
1. **Optional realm override**, defaulting to `hecate_om_identity:realm()` — confirmed a real deployed need by `hecate-robo-rumbler`, `hecate-biotope`/`hecate-society`, and `hecate-dronex` (piece B), all publishing to a second, non-fleet realm off the same pool. `macula_publisher:start_link/6` already accepts `Realm` as a plain argument, so this is purely "don't hardcode it in the wrapper," not new SDK work.
2. **A caller-selectable outcome-handling mode** (`sync` — block and return `ok | {error, _}`; `async_log`; `async_silent`, the current default nobody asked for but most callers use) implemented as three tiny stock `handle_published/2` bodies. `hecate-grid`/spartan mostly want `async_silent`; `hecate-victron`/`hecate-warden`/`hecate-sentinel` want `async_log`; `hecate-biotope`/`hecate-society`/`hecate-mpong-bot` want `sync` (they return `{error,_}` to their own caller today, meaning they're already blocking — `sync` mode just moves that blocking into the wrapper).
3. **N-topic fan-out convenience.** `hecate-news` publishes one fact to up to 4 topics per call. Add `publish_many/2,3` (loop `publish/2,3` per topic) rather than complicating the base signature — `macula_publisher` itself is single-topic by design and shouldn't be bent to fit this.

**Closest existing prior art, worth reading directly as a skeleton for the wrapper's *outer* API (not for how it talks to the mesh — that part should go through `macula_publisher`, which none of these do):** `hecate-mpong-bot/src/hecate_mesh.erl` — already a small, generic publish/subscribe/unsubscribe facade over pool+realm with degrade-to-`{error, mesh_unavailable}` semantics.

**Migration payoff:** once this ships, every one of the ~20+ hand-rolled `catch macula:publish(...)` call sites found this session (spartan's ~10, parksim's 6, grid/news/victron/warden/biotope/society/mpong-bot/archive/sentinel/robo-rumbler/dronex/tom-ocean/tom-world) becomes a direct one-line replacement — see the migration table in the root doc.

**Acceptance:** existing publish call sites in at least one already-hand-rolled repo (spartan or parksim give the most call sites to prove the abstraction against) can be ported to `hecate_om_pubsub:publish/2,3` with a net line reduction and no behavior change, verified by that repo's own tests still passing. Additionally verify the realm-override and each outcome-handling mode against a repo that actually needs it (dronex/biotope for realm; biotope/victron for `sync`/`async_log`) — don't just prove it against the `async_silent` default, since that's the minority-need case among real callers, not the representative one. Also verify the free lifecycle facts (`pubsub.publish_started_v1`/`_completed_v1`) actually land on the mesh — that's a new, testable capability this migration adds, not just a refactor.

### D. Declarative `subscriptions/0` + `hecate_om_pubsub:ensure_subscriptions/1`

**RE-CORRECTED, 2026-08-24 session 2 (second pass) — same mistake as piece C, now fixed with the SDK source read directly.** `src/pubsub/macula_subscriber.erl` and `src/client/macula_client_replay.erl` were read this session, not assumed. They settle nearly all of what the first pass treated as an open reconnect-strategy design question:

1. **Ordinary link churn is already invisible to a subscriber.** `macula_client_replay:subs_to/2` mirrors `advs_to/2` exactly: on every link respawn, the pool re-issues a SUBSCRIBE frame for every `{Realm, Topic}` it's tracking, using the *same* `SubRef`. A `macula_subscriber` process never even learns a respawn happened — no message, no need to react. This means most of the hand-rolled reconnect machinery found this session (spartan's push-on-`macula_event_gone` in 5 of 6 modules, `tom_ocean_mesh.erl`'s pool-`DOWN` monitor) is defending against something the pool already resolved one layer down — it's not a design template, it's a list of code that becomes redundant.
2. **`macula_subscriber` turns a *genuine* subscription loss into a process stop**, not a message to react to: `handle_info({macula_event_gone, SubRef, Reason}, State) -> {stop, Reason, State}`. Under a plain OTP supervisor with a `transient`/`permanent` child, that's the entire reconnect story — the supervisor restarts the child, `init/1` re-subscribes. This is "let it crash," already the house style per `macula-io/CLAUDE.md`, and it's *less* code than any hand-rolled push/pull hybrid, not a third option to weigh against them. `federation_inbox`'s own pull-timer reconcile and `committee.erl`'s resubscribe-double-fire guard read as workarounds for not having this — worth flagging back to `hecate-spartan` as a simplification once `hecate_om_pubsub` exists, not just a design input.
3. **Dynamic topic sets (federation_inbox's real, still-standing requirement) are a supervision-membership problem, not a reconnect problem.** `hecate_om_pubsub` should run its `macula_subscriber` children under a dynamic supervisor; `ensure_subscriptions/1` diffs the caller's current desired `{Topic, HandlerMod, Args}` set against running children and `start_child`s/`terminate_child`s the delta. `subscriptions/0` covers the static case (called once at boot); `ensure_subscriptions/1` is what `federation_inbox`-shaped services call again whenever their desired set changes (e.g. on `entity_registered`) — same mechanism, no separate reconnect logic layered on top.
4. **Wildcard topics** (`hecate-parksim`'s `<<"energy/+/grid_price">>`) pass straight through to `macula:subscribe/4,5` inside `macula_subscriber` — nothing for the wrapper to special-case.
5. **Caller-owns-resubscribe (`hecate-mpong-bot`, `hecate-robo-rumbler`) becomes moot, not a design choice to preserve.** Their stated reason — "there is no central subscription manager" — was true only because `hecate_om` didn't offer one; once it wraps `macula_subscriber` + supervision (which is *less* code than what they hand-rolled), the deliberate-caller-ownership stance loses its rationale. Still fine to let a service opt out (start its own `macula_subscriber` directly), just not a requirement to preserve two supported paths.
6. **Dedup stays out of scope, correctly** — every dedup implementation found (`post_id`, `{recipient,msg_id}`, a bounded eviction window keyed on `{source,dataset,epoch,seq}`, deterministic sha256 aggregate IDs) is domain-specific and belongs in the consumer's `handle_event/4`, not in `hecate_om_pubsub`.
7. **Heartbeat-as-liveness-beacon** (`hecate-sentinel`) is now clearly a business-level concern (detect a *silently unresponsive peer*, a different problem than "did my subscription survive"), not a piece D mechanism — leave it to services that want it, don't fold it in.

**CONFIRMED LIVE, 2026-08-24 — not just a code read.** Wrote and ran `macula_link_respawn_replay_tests.erl` against real `macula_client`/`macula_station_link` processes (no station, no QUIC — same unreachable-seed technique `macula_client_tests.erl` already uses): subscribed, captured the link pid via `macula_client:links/1`, killed it, and watched the pool respawn. Result — **no `macula_event_gone` fired**; `macula_station_link:subscribe/4` was traced (via `meck:history/1`, not inferred) to have been called again against the new link with the same `{Realm, Topic, Pool}`; a synthetic event injected via the new link reached the original subscriber under the unchanged `SubRef`, with zero action from the subscribing process. 2.5s, passed. File currently sits **uncommitted** in `macula-io/macula/test/` — it fills a real, confirmed-absent gap (no existing test exercised `on_respawn_link`/`subs_to/2`'s live behavior) and is a candidate to become a permanent regression test in the SDK; needs the user's sign-off to commit, not assumed.

**Acceptance:** a service declaring `subscriptions/0` receives events in its handler module without ever touching a raw `{macula_event, ...}` message. A link respawn (station-side connection drop, pool auto-reconnects) is verified to leave the subscriber process running and receiving events *without restarting* — proving the wrapper isn't reimplementing what the pool already does. A genuine subscription loss (pool close) is verified to restart the child via ordinary supervision and resume receiving events, with no bespoke reconnect code in `hecate_om_pubsub` itself. `ensure_subscriptions/1` is verified against a dynamic set (modeled on `federation_inbox`'s add-a-topic-at-runtime pattern), confirming it starts exactly the new child and touches nothing already running.

### E. `hecate_om_content:put/1,2` / `get/1,2`

**What:** Synchronous wrappers around `macula_feeder`/`macula_download`'s async callback shape, matching what an HTTP handler actually wants (block with a timeout, get bytes or an MCID back).

**Derive from `hecate-tube/apps/guide_tube_lifecycle/src/tube_content_put.erl` and `tube_content_get.erl` directly.** These carry a second hard-won, confirmed-live lesson that MUST be preserved, not just the boilerplate: `macula_download`'s direct-dial path (`start_link_direct`) only resolves content that has a `content_announcement` DHT record, which only chunked content gets. Small blobs (a logo, a thumbnail) never get one, so direct-dial 404s even immediately after upload — confirmed live on beam02. The *pooled* `start_link/4,5` has to be used instead, and put/get sides have to agree on that. Getting this wrong in a shared wrapper actively hurts every future consumer rather than just adding boilerplate, so this is worth building carefully rather than deferring — this is exactly the kind of "a service is already reinventing it" trigger the original survey said to wait for, and it comes with a real bug-avoidance payload attached, not just line-count reduction.

**Acceptance:** round-trips a small (non-chunked) blob through the pooled path and gets it back; a test explicitly asserts it does NOT use `start_link_direct` for small content (regression-proof the lesson, don't just document it in a comment).

### F. Shared wire-args decode helper

**What:** A small, shared normalizer for the RPC/stream `args`-arrive-as-atom-keys-not-binary gotcha (macula's frame decoder round-trips through `binary_to_existing_atom/1`). Currently re-documented and re-solved independently in every provider desk (`hecate-tube`'s `stream_video_clip_by_id.erl` and `advertise_channel_lookup.erl` both do it separately).

**Where it lives:** probably a function on `hecate_om_capabilities` (or wherever B's provider-side dispatch lands, since that's the natural place a handler's args pass through before reaching the service's own handler function) rather than a new module — small enough not to warrant one.

**Acceptance:** a provider handler written against it doesn't need its own atom/binary tolerance code; existing hand-rolled tolerance code in at least one already-hand-rolled repo can be deleted in favor of it.

---

## Sequencing

1. **C** (publish) — smallest, lowest risk, immediate value, no open design questions. **DONE 2026-08-24.**
2. **B** (RPC provider wiring, streaming half deferred) — done out of original order once the design question resolved into "replace hand-rolled record-writing with `advertise_direct`," which turned out not to need the embedder/llm survey to settle — the answer came from reading `hecate_om_capabilities`'s own existing consumer side (`call_capability`/`call_station`) plus `macula_response.erl` directly. **DONE 2026-08-24 (RPC half; streaming deferred, see piece B).**
3. **F** (decode helper) — small, unblocks cleaner examples for E; also now useful for B's handler bodies.
4. **E** (content) — self-contained, real bug-avoidance payload, no dependency on B/D.
5. **D** (declarative subscriptions) — blocked on the handover's instructed survey of `hecate-spartan` first (done, see piece D above — design is settled, not yet implemented).
6. **A** (connection lifecycle migration) — last, deliberately isolated, full regression suite from 1–5 as the safety net.

## Testing strategy

Mirror this session's barrel-read-model work and hecate-tube's own smoke-test recipe (`hecate-tube/plans/PLAN_HECATE_TUBE_ROOT.md`, "Smoke-test harness" section): `rebar3 compile` + `rebar3 eunit`/`ct` catch logic bugs, but wiring bugs (event-shape mismatches, boot-order races, a supervised child that doesn't restart the way expected) only show up on a real, live boot. Where a real station is needed, use `hecate-spartan`'s proven throwaway-live-mesh technique (`run-throwaway-mind.sh` + `remote_console`) rather than inventing a new one; for in-memory round-trips without a real station, `hecate-testkit`'s `with_mesh/1,2` + `publish/3`/`subscribe/2`/`await/2,3` (found this session, only `hecate_om_identity` is meck'd, not macula itself) is real infrastructure and should become `hecate_om`'s own test dependency for piece C/D's suites, not reinvented. Every acceptance criterion above that says "verify X actually happens" means a live-boot or `with_mesh` round-trip, not a callback-registration assertion — the CT suite added for barrel read models this session (a genuine round-trip, not a mock) is the bar to match. Piece D specifically needs a link-respawn-vs-pool-close distinction test (see piece D's acceptance) — that can only be proven live or under `with_mesh`'s real (if in-memory) subscribe path, not by unit-testing `hecate_om_pubsub`'s own code in isolation.

## Two new candidate pieces surfaced by the 2026-08-24 session-2 survey (not yet scoped, not in A-F)

- **G. RPC-outcome retryability helper.** `hecate-tom-world`'s `tom_wire_macula.erl` builds a `{ok,_} | {refused,_} | {unreachable,_}` classification by asking `macula_bolt4:is_retryable/1` rather than duplicating the BOLT#4 code table locally. The response-side analog of piece F (which only covers request-arg decoding). Small, likely folds into the same module as F.
- **H. Non-raising `hecate_om`/`hecate_om_identity` accessors.** `macula_client()`/`realm()` currently raise `noproc` when called before boot. Three independent repos (`hecate-biotope`, `hecate-society`, `hecate-dronex`) hand-roll try/catch to convert this to `{error, not_booted}`; `hecate-tom-world`'s `tom_advertiser.erl` doesn't, and would crash if called at the wrong time. Fixing this once in `hecate_om` obsoletes the hand-rolled defense everywhere and directly informs piece A's open boot-race question — the race being defended against today, independent of whether release-boot ordering actually allows it.

## Migration targets — hand-rolled code each piece makes replaceable

This is the second half of the plan's actual purpose: not just "what does `hecate_om` grow," but "what gets deleted once it does." Every module below is a candidate to be ported to call the new wrapper and have its own mesh-plumbing code removed, once the corresponding piece ships and is proven against it (see each piece's acceptance criteria).

| Piece | Hand-rolled code it replaces |
|---|---|
| **C** (publish) | `hecate-spartan` (~10 call sites across `federation_*.erl`, `committee*.erl`, 5 `maybe_*.erl` handlers), `hecate-parksim` (6 `emit_*.erl`), `hecate-grid/-news/-victron/-warden/-biotope/-society` (1 module each), `hecate-mpong-bot`'s `hecate_mesh.erl` publish half, `hecate-robo-rumbler`'s `rumble_mesh.erl` publish half, `hecate-archive`'s `hecate_archive_facts.erl`, `hecate-sentinel`'s `publish_enriched_fact.erl`/`broadcast_threat_digest.erl`, `hecate-tom-ocean`'s `tom_ocean_mesh.erl` publish half, `hecate-tom-world`'s `tom_crier.erl`, `hecate-dronex`'s `dronex_mesh.erl` publish half. |
| **D** (subscribe) | `hecate-spartan`'s 6 `federation_*`/`inhabit_mind/*` subscribe modules (with the specific benefit of deleting `committee.erl`'s resubscribe-double-fire guard, no longer needed), `hecate-archive`'s `collect_observations.erl`, `hecate-parksim`'s `on_grid_price_changed_schedule_charging.erl`, `hecate-sentinel`'s `ingest_warden_reports.erl`, `hecate-mpong-bot`'s `hecate_mesh.erl` subscribe half, `hecate-robo-rumbler`'s `rumble_mesh.erl` subscribe half, `hecate-tom-ocean`'s `tom_ocean_mesh.erl` subscribe half, `hecate-tom-player`'s `tom_wire_macula.erl`. |
| **B** (provider) | The `*_mesh_rpc.erl` module itself in `hecate-dns`/`-git`/`-llm`/`-rag` (the shared `advertise_all/0`→`handler_table/0`→`route/2`→`delegate/3` skeleton becomes unnecessary, not just its boilerplate), `hecate-embedder`'s `hecate_embedder_advertiser.erl`, `hecate-tube`'s `tube_mesh_providers.erl` (RPC half — its streaming half still needs `advertise_direct`, a genuinely different primitive), `hecate-tom-world`'s `tom_advertiser.erl`, `hecate-dronex`'s `dronex_mesh.erl` provider half. |
| **F/G** (decode/retryability helpers) | Atom/binary tolerance code in `hecate-embedder` (`gf/2`), and — pending the live check below — potentially `hecate-dns`/`-git`/`-llm`/`-rag`'s unguarded `route/2` clauses if they turn out to need it. `hecate-tom-world`'s `classify/1` retryability table (piece G). |
| **H** (non-raising accessors) | try/catch in `hecate-biotope`, `hecate-society`, `hecate-dronex` around `hecate_om_identity:macula_client()` — becomes dead code once the accessor itself stops raising. |

Not every module disappears entirely — several (spartan's `federation_inbox`, `committee.erl`) keep real business logic (dedup, turn-taking) that stays in the consuming service; only the mesh-plumbing half is deleted.

## Not yet decided — needs sign-off before any of B or D gets written

- ~~`capabilities()` handler field vs. separate `capability_handlers/0` callback~~ — **DECIDED, implemented**: inline `handler => {Module, Args}` field on the existing `capability()` map, per the evidence (two empty stubs, one hand-duplicated list, one name mismatch — all three sources-of-truth failures this shape avoids).
- Whether RPC and streaming handlers share one declaration shape — still open; streaming was deliberately left out of this implementation pass (see piece B's "not yet done" note) for lack of a second exemplar to design against.
- MFA-pair (not closure) handler registration — **should be treated as settled**, not open: `tom_advertiser.erl`'s hot-code-upgrade reasoning is concrete and the four `*_mesh_rpc.erl` services already do it correctly.
- `subscriptions/0`'s tuple shape (`{Topic, HandlerMod, Args}`) itself is now settled — reconciliation and reconnect handling turned out to be a supervision-design question (piece D above), not a tuple-shape question. Whether `Args` is required or defaultable is the one remaining small item.
- `hecate_om_pubsub:publish/2,3`'s three outcome-handling modes (`sync`/`async_log`/`async_silent`) and optional realm parameter — settled in shape (piece C above); whether `async_silent` or `async_log` is the *default* when unspecified is the one remaining small item (survey leans `async_silent`, matching the majority of real call sites).
- Whether the README's "outbound only... not a network library" line needs updating now that this is a second, larger wrap beyond RPC-consumer (unchanged from the original survey's open question — still open).

## Where to go next

Design is now grounded directly in the SDK's supervised-behaviour source (`macula_publisher.erl`, `macula_subscriber.erl`, `macula_client_replay.erl`), not inferred from hand-rolled call sites — read `plans/PLAN_HECATE_OM_MESH_WRAPPERS_ROOT.md`'s "Survey findings" section for the full per-repo detail behind each piece above. **Both items that previously needed a live-boot check are now closed, 2026-08-24:**

- ~~Which `advertise` flavor do `tom_advertiser.erl` and `dronex_mesh.erl` actually use?~~ — **resolved by a plain code read**, no live boot needed: both call `macula:advertise/4,5` (`tom_advertiser.erl:78`, `dronex_mesh.erl:98`), not `advertise_direct`. Per the corrected fact above, **neither repo carries the reconnect-loss bug** — both are safe as-is, same as `dns`/`git`/`llm`/`rag`.
- ~~Does an ordinary link respawn actually leave a subscription untouched?~~ — **resolved live**, see piece D above: confirmed with a real running test (`macula_link_respawn_replay_tests.erl`), not just a code read.

Nothing blocking design remains open. What's left is implementation, plus the small sign-offs in "Not yet decided" above (defaults, not architecture).
