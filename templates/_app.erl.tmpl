%%% @doc {{service_name}} OTP application entry.
-module({{erlang_app_name}}_app).
-behaviour(application).

-export([start/2, stop/1]).

start(_StartType, _StartArgs) ->
    hecate_om:boot({{erlang_service_module}}).

stop(_State) ->
    ok.
